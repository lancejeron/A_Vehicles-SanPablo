//- This is how we extend a template using the 'extends' keyword
extends ../../../templates/master.pug
//- This is how we insert dynamic content to extended templates
block content
    br
    h1.title Welcome to Kotse Mo to!
    hr

    ul
        //- This is how we iterate through arrays and access
        //- their element
    //- each user in users
    //-          li= `${user.id} - ${user.make} - ${user.model} - ${user.year} - ${user.plate_number} - ${user.vcondition}`
         
    p 
        a(class='button', href='/view') VIEW &nbsp &nbsp 
        a(class='button', href='/users/new') ADD &nbsp &nbsp 
        a(class='button', href='/delete') DELETE &nbsp &nbsp 
        a(class='button', href='/update') UPDATE 
